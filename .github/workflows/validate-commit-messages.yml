name: Validate PR Title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  validate-pr-title:
    name: Validate PR Title
    runs-on: ubuntu-latest
    steps:
      - name: Check PR title matches release pattern
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          # Matches the headerPattern in .releaserc.json.
          # Type must EITHER be wrapped in brackets [type] OR followed by a colon type:
          # Optionally preceded by a Jira ticket prefix (e.g. SF-5310: )
          #
          # Valid examples:
          #   feat: add new feature
          #   feat(scope): add new feature
          #   [minor] add new feature
          #   [minor](scope) add new feature
          #   SF-5310: [minor] add constant for pairing hearing aids
          #   SF-5310: feat(scope): add new feature
          #
          # Invalid examples:
          #   sadd PR title validation workflow  ← bare word with no colon or brackets
          # Type must EITHER be wrapped in brackets [type] OR be followed by a colon type:
          PATTERN='^([A-Za-z]{2,4}-[0-9]{1,5}:[[:space:]]+)?(\[[a-zA-Z0-9_]+\](\([^)]+\))?[[:space:]]+|[a-zA-Z0-9_]+(\([^)]+\))?:[[:space:]]+).+'

          echo "PR title: $PR_TITLE"

          if echo "$PR_TITLE" | grep -qE "$PATTERN"; then
            echo "✅ PR title is valid"
          else
            echo "❌ PR title does not match the required format."
            echo ""
            echo "Expected format (matching .releaserc.json headerPattern):"
            echo "  [Jira ticket: ] <type>[(<scope>)][: | ] <subject>"
            echo ""
            echo "Valid examples:"
            echo "  feat: add new feature"
            echo "  fix(auth): resolve login issue"
            echo "  [minor] improve performance"
            echo "  SF-5310: [minor] add constant for pairing hearing aids"
            echo "  SF-5310: feat(scope): add new feature"
            exit 1
          fi



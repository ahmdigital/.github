name: Notify PR is ready for review

on:
  pull_request:
    types: [labeled]

permissions:
  contents: read
  pull-requests: read

jobs:
  notify:
    uses: ahmdigital/ahm-github-workflows/.github/workflows/pr-ready-slack-notification.yml@46a1c0108e5c5cfbb415bb5c9a20ba76893243b3
    secrets:
      slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
      gh-app-id: ${{ secrets.GH_APPLICATION_ID }}
      gh-app-private-key: ${{ secrets.GH_APPLICATION_PRIVATE_KEY }}
